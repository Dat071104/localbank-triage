{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the LocalBank triage window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
